# fedora-init

# install prereqs

```
sudo dnf -y update
sudo dnf -y install snapd ansible
```

# reboot the system

# run ansible playbook

```
sudo ansible-playbook fedora_init.yaml
```

# reboot the system
